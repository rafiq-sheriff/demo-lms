import type { NextConfig } from "next";

/**
 * LMS API proxy: `src/app/api/lms/[...path]/route.ts` (avoids brittle rewrites / empty BACKEND_URL).
 */
const nextConfig: NextConfig = {};

export default nextConfig;
